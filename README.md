By default you shoul keep your index file in `./public` directory.

You can change virtual host settings in file `000-default.conf`. It is bind mounted if you used `./run.sh` script. Restart container after config change.